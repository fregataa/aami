global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'aami-cluster'
    environment: 'development'

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

# Load alert rules
rule_files:
  - /etc/prometheus/rules/*.yml

# Scrape configurations
scrape_configs:
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          service: 'prometheus'

  # Alertmanager monitoring
  - job_name: 'alertmanager'
    static_configs:
      - targets: ['alertmanager:9093']
        labels:
          service: 'alertmanager'

  # Grafana monitoring
  - job_name: 'grafana'
    static_configs:
      - targets: ['grafana:3000']
        labels:
          service: 'grafana'

  # Node Exporter - System metrics
  # Uses HTTP Service Discovery from Config Server
  - job_name: 'node-exporter'
    http_sd_configs:
      - url: 'http://config-server:8080/api/v1/sd/prometheus/active?exporter_type=node_exporter'
        refresh_interval: 30s
    relabel_configs:
      # Preserve original instance label
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: localhost:9100  # Node Exporter default port

  # DCGM Exporter - GPU metrics
  # Uses HTTP Service Discovery from Config Server
  - job_name: 'dcgm-exporter'
    http_sd_configs:
      - url: 'http://config-server:8080/api/v1/sd/prometheus/active?exporter_type=dcgm_exporter'
        refresh_interval: 30s
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance

  # Custom Exporters
  # Uses HTTP Service Discovery from Config Server
  - job_name: 'custom-exporters'
    http_sd_configs:
      - url: 'http://config-server:8080/api/v1/sd/prometheus/active?exporter_type=custom'
        refresh_interval: 30s
